/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [{
        protocol: 'https',
        hostname: 'hwcfhhwvgncuxmzisqti.supabase.co'
      }],
    }
};

export default nextConfig;
